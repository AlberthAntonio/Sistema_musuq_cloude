# Backend API - Sistema Musuq Cloud
# Instalar: pip install -r requirements.txt

# ---- Framework principal ----
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# ---- Base de datos ----
sqlalchemy>=2.0.23
psycopg2-binary>=2.9.9    # PostgreSQL (producción)
# aiosqlite>=0.20.0        # Si se usa SQLite async

# ---- Migraciones ----
alembic>=1.13.0

# ---- Autenticación / Seguridad ----
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
python-multipart>=0.0.6   # Form data (login OAuth2)

# ---- Validación / Serialización ----
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ---- Cliente HTTP (para integración desktop) ----
httpx>=0.26.0

# ---- Producción ----
gunicorn>=21.2.0           # WSGI server para detrás de nginx
python-json-logger>=2.0.7  # Logging JSON estructurado
